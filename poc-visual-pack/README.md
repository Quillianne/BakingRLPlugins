# POC Visual Pack

Validation contributor plugin for Overlay Studio.

- Depends on `bakingrl.poc-overlay-studio`.
- Contributes `demo-score-widget` to `overlay-studio.visual` through metadata, resources, and service APIs.
- Exposes its own `visual-pack.content` extension point.
- Reads downstream content pack contributions through the host-mediated extension API.
